CREATE TABLE NOTE
(
	ID INT AUTO_INCREMENT,
	TEAM_MEMBER VARCHAR(255),
	PROJECT VARCHAR(255),
	WORK_YESTERDAY VARCHAR(2000),
	WORK_TODAY VARCHAR(2000),
	IMPEDIMENTS VARCHAR(2000),
	CREATED_ON TIMESTAMP,
	MODIFIED_ON TIMESTAMP
);